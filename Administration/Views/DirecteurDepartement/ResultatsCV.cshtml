@model List<Administration.Models.Match>

@{
    ViewData["Title"] = "Résultats CV & Décisions";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var offres = ViewBag.Offres as List<Administration.Models.OffreEmploi> ?? new();
    int? selectedId = ViewBag.SelectedOffreId;

    var offreSelectionnee = selectedId.HasValue
        ? offres.FirstOrDefault(o => o.Id == selectedId.Value)
        : null;
}

<div class="container-fluid px-4 py-3">

    <!-- FILTRE -->
    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form method="get">
                <label class="form-label">Sélectionner un poste</label>
                <select name="offreId" class="form-select" onchange="this.form.submit()">
                    <option value="">-- Choisir --</option>
                    @foreach (var o in offres)
                    {
                        <option value="@o.Id" selected="@(selectedId == o.Id)">
                            @o.Titre — @o.Departement
                        </option>
                    }
                </select>
            </form>
        </div>
    </div>

    @if (selectedId.HasValue && offreSelectionnee != null)
    {
        <!-- DÉTAILS DU POSTE -->
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-white fw-semibold">
                Détails du poste : @offreSelectionnee.Titre
            </div>

            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <dl class="row mb-0">
                            <dt class="col-sm-4">Département</dt>
                            <dd class="col-sm-8">@offreSelectionnee.Departement</dd>

                            <dt class="col-sm-4">Type</dt>
                            <dd class="col-sm-8">@offreSelectionnee.Type</dd>

                            <dt class="col-sm-4">Expérience requise</dt>
                            <dd class="col-sm-8">@offreSelectionnee.Experience an(s)</dd>
                        </dl>
                    </div>

                    <div class="col-md-6">
                        <dl class="row mb-0">
                            <dt class="col-sm-4">Niveau d'éducation</dt>
                            <dd class="col-sm-8">@offreSelectionnee.NiveauEducation</dd>

                            <dt class="col-sm-4">Statut</dt>
                            <dd class="col-sm-8">
                                <span class="badge @(offreSelectionnee.Statut == "ACTIF" ? "bg-success" : "bg-secondary")">
                                    @offreSelectionnee.Statut
                                </span>
                            </dd>

                            <dt class="col-sm-4">Date de création</dt>
                            <dd class="col-sm-8">@offreSelectionnee.DateCreation.ToString("dd/MM/yyyy")</dd>
                        </dl>
                    </div>
                </div>

                @if (!string.IsNullOrWhiteSpace(offreSelectionnee.Description))
                {
                    <div class="mt-3">
                        <strong>Description :</strong>
                        <p class="mt-1">@offreSelectionnee.Description</p>
                    </div>
                }
            </div>
        </div>
    }

    <!-- RÉSULTATS -->
    @if (Model != null && Model.Any())
    {
        <div class="card shadow-sm">
            <div class="card-header bg-white">
                <strong>Candidats pour @offreSelectionnee?.Titre</strong>
                <span class="badge bg-secondary ms-2">@Model.Count candidat(s)</span>
            </div>

            <div class="card-body p-0">
                <div class="table-responsive">
<<<<<<< HEAD
                    <table class="table table-hover mb-0" id="resultsTable">
=======
                    <table class="table table-hover mb-0">
>>>>>>> 364b0a3c7128899c3df393e4339086a493d07ccc
                        
                        <!-- HEADER -->
                        <thead class="table-light">
                            <tr>
                                <th>#</th>
                                <th>Candidat</th>
                                <th>Email</th>
                                <th>Téléphone</th>
                                <th>Compétences</th>
                                <th>Diplôme</th>
                                <th>Expérience</th>
                                <th>Score Global</th>
                                <th>Actions</th> <!-- NEW -->
                            </tr>
                        </thead>

                        <tbody>
                            @{
                                int rank = 1;
                            }

                            @foreach (var m in Model)
                            {
                                <tr>
                                    <td><strong>@rank</strong></td>

                                    <td>@(m.Cv?.NomCandidat ?? "—")</td>
                                    <td>@(m.Cv?.Email ?? "—")</td>
                                    <td>@(m.Cv?.Telephone ?? "—")</td>

                                    <!-- COMPETENCE -->
                                    <td>
                                        <div class="progress" style="height:6px;">
                                            <div class="progress-bar bg-info" style="width:@m.CompetenceScore%"></div>
                                        </div>
                                        <small>@m.CompetenceScore.ToString("0")%</small>
                                    </td>

                                    <!-- DIPLOME -->
                                    <td>
                                        <div class="progress" style="height:6px;">
                                            <div class="progress-bar bg-warning" style="width:@m.DiplomeScore%"></div>
                                        </div>
                                        <small>@m.DiplomeScore.ToString("0")%</small>
                                    </td>

                                    <!-- EXPERIENCE -->
                                    <td>
                                        <div class="progress" style="height:6px;">
                                            <div class="progress-bar bg-success" style="width:@m.ExperienceScore%"></div>
                                        </div>
                                        <small>@m.ExperienceScore.ToString("0")%</small>
                                    </td>

                                    <!-- SCORE -->
                                    <td>
                                        <span class="badge bg-dark">
                                            @m.GlobalScore.ToString("0.0")%
                                        </span>
                                    </td>

                                    <!-- ACTIONS -->
                                    <td>
                                        <div class="d-flex gap-1">

                                            <!-- VALIDER -->
                                            <form method="post" asp-action="UpdateCandidateValidationStatus">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="cvId" value="@m.CvId" />
                                                <input type="hidden" name="offreId" value="@selectedId" />
                                                <input type="hidden" name="status" value="Validated" />
                                                <button type="submit" class="btn btn-success btn-sm">
                                                    ✔
                                                </button>
                                            </form>

                                            <!-- REJETER -->
                                            <form method="post" asp-action="UpdateCandidateValidationStatus">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="cvId" value="@m.CvId" />
                                                <input type="hidden" name="offreId" value="@selectedId" />
                                                <input type="hidden" name="status" value="Rejected" />
                                                <button type="submit" class="btn btn-danger btn-sm">
                                                    ✖
                                                </button>
                                            </form>

                                        </div>
                                    </td>

                                </tr>

                                rank++;
                            }

                        </tbody>
                    </table>
                </div>
            </div>
<<<<<<< HEAD
            <!-- Pagination -->
            <div class="card-footer bg-white d-flex justify-content-between align-items-center">
                <div class="text-muted small" id="paginationInfo">
                    Affichage de <strong>1 à @Model.Count</strong> sur <strong>@Model.Count</strong> candidats
                </div>
                <nav>
                    <ul class="pagination pagination-sm mb-0" id="paginationControls">
                        <!-- Generated by JS -->
                    </ul>
                </nav>
            </div>
=======
>>>>>>> 364b0a3c7128899c3df393e4339086a493d07ccc
        </div>
    }
    else if (selectedId.HasValue)
    {
        <div class="alert alert-info">
            Aucun CV trouvé pour ce poste.
        </div>
    }
    else
    {
        <div class="text-center text-muted py-5">
            Sélectionnez un poste pour afficher les résultats.
        </div>
    }

<<<<<<< HEAD
</div>

@section Scripts {
    <script>
        const itemsPerPage = 10;
        let currentPage = 1;
        let allRows = [];

        document.addEventListener('DOMContentLoaded', function() {
            const table = document.getElementById('resultsTable');
            if (table) {
                const tr = table.getElementsByTagName('tbody')[0].getElementsByTagName('tr');
                allRows = Array.from(tr);
                renderPagination();
                showPage(1);
            }
        });

        function renderPagination() {
            const totalPages = Math.ceil(allRows.length / itemsPerPage);
            const container = document.getElementById('paginationControls');
            if (!container) return;
            container.innerHTML = '';
            
            if (totalPages === 0) {
                document.getElementById('paginationInfo').innerHTML = `Affichage de <strong>0 à 0</strong> sur <strong>0</strong> candidats`;
                return;
            }

            // Prev
            const prevLi = document.createElement('li');
            prevLi.className = `page-item ${currentPage === 1 ? 'disabled' : ''}`;
            prevLi.innerHTML = `<a class="page-link" href="javascript:void(0)" onclick="if(${currentPage} > 1) showPage(${currentPage - 1})">Précédent</a>`;
            container.appendChild(prevLi);

            // Pages
            for (let i = 1; i <= totalPages; i++) {
                const li = document.createElement('li');
                li.className = `page-item ${currentPage === i ? 'active' : ''}`;
                li.innerHTML = `<a class="page-link" href="javascript:void(0)" onclick="showPage(${i})">${i}</a>`;
                container.appendChild(li);
            }

            // Next
            const nextLi = document.createElement('li');
            nextLi.className = `page-item ${currentPage === totalPages ? 'disabled' : ''}`;
            nextLi.innerHTML = `<a class="page-link" href="javascript:void(0)" onclick="if(${currentPage} < ${totalPages}) showPage(${currentPage + 1})">Suivant</a>`;
            container.appendChild(nextLi);
        }

        function showPage(page) {
            currentPage = page;
            allRows.forEach(row => row.style.display = 'none');
            
            const totalItems = allRows.length;
            if (totalItems === 0) return;

            const start = (page - 1) * itemsPerPage;
            const end = Math.min(start + itemsPerPage, totalItems);

            for (let i = start; i < end; i++) {
                allRows[i].style.display = '';
            }

            document.getElementById('paginationInfo').innerHTML = `Affichage de <strong>${start + 1} à ${end}</strong> sur <strong>${totalItems}</strong> candidats`;
            renderPagination();
        }
    </script>
}
=======
</div>
>>>>>>> 364b0a3c7128899c3df393e4339086a493d07ccc
